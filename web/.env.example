# Frontend runtime + dev proxy API base (no VITE prefix required):
BACKEND_API_BASE_URL=https://api.runwayalgo.com/backend/public/api/v1
# Optional local backend override:
# BACKEND_API_BASE_URL=http://127.0.0.1:8000/api/v1

# Reverb live updates (set these in deploy env when websocket is enabled)
# VITE_REVERB_APP_KEY=
# VITE_REVERB_HOST=
# VITE_REVERB_PORT=443
# VITE_REVERB_SCHEME=https
# VITE_ENABLE_REALTIME=true
